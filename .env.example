# Tycho Atomic Arbitrage - Environment Configuration
# =================================================
# 1. Copy this file to '.env' in the same directory
# 2. Fill in your actual values in the '.env' file
# 3. Keep '.env' in your .gitignore to protect secrets

# Required Configuration
# ----------------------

# Blockchain RPC endpoint for the target chain
TYCHO_RPC_URL=your_ethereum_rpc_endpoint

# Tycho API credentials
TYCHO_API_KEY=your_tycho_api_key

# Private key for transaction execution (without 0x prefix)
TYCHO_EXECUTOR_PRIVATE_KEY=your_private_key_without_0x_prefix

# Optional Configuration
# ----------------------

# Target blockchain (ethereum, base, unichain)
TYCHO_CHAIN=ethereum

# Minimum pool TVL threshold in native currency (ETH, etc.)
TYCHO_TVL_THRESHOLD=70.0

# Minimum profit threshold in basis points (100 = 1%)
TYCHO_MIN_PROFIT_BPS=100

# Slippage tolerance in basis points (500 = 5%)
TYCHO_SLIPPAGE_BPS=500

# MEV bribe percentage (0-100, where 99 = 99% of profit)
TYCHO_BRIBE_PERCENTAGE=99

# Flashbots identity key for MEV bundle submission (optional)
TYCHO_FLASHBOTS_IDENTITY_KEY=your_flashbots_identity_key_without_0x_prefix